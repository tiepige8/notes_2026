最近在做企业内部提效的事情。背景是我们是一家相对传统的电商企业，主要以短视频和直播带货为主，团队人才密度不算高，目前正在推进 AI 转型。

整体转型主要有几个方向：

1. AI 工程化：快速实现工程化能力，比如自动化投放、自动化剪辑、素材上传与发布等
2. AI 辅助决策：这块目前做得还比较弱
3. AI 视频化：用 AI 生成视频来辅助带货

我主要想聊的是 AI 工程化。因为我本身不是研发出身，之前主要做产品和运营，算是借助 AI 实现了能力的跨越。

做自动化通常离不开两种常规路径：
• 平台开放 API：优势是稳定，官方维护且千锤百炼，更新频次低；但很多能力平台并不开放。
• 浏览器自动化（如 Playwright）：通过模拟真人点击来操作，可控性很强，但遇到动态核验审查极严的平台（比如微信），往往很难正常跑通。

综合跑下来，我最近摸索出一个更高效的方案：通过抓包工具拦截平台的内部 API。

它的底层逻辑和浏览器自动化类似，相当于"浏览器自动化 + API"的结合体。有了抓包之后，所有的前端行为本质上都是 API 的调用与返回。

核心只需要搞定两件事：

1. 登录态：保存请求时的身份校验信息。
2. API 端点与返回值：理清接口的具体参数和返回数据，让 AI 辅助分析和拦截，跑起来其实很快。

这种方案的核心价值，在于能把很多针对 C 端的工具直接用到 B 端场景。比如很多剪辑软件包装了非常方便的剪辑能力，但不会对 B 端开放 API，或者收费极其昂贵。通过拦截接口的形式，前期稍微花点时间理清接口，就能直接接入使用。

理论上这种方案最大的问题是不够稳定：平台一旦做前端或后端迭代改了 API，这边可能就会失效。但我实际用下来发现改动频率其实很低，因为整体重构 API 逻辑对大平台来说风险和成本都极高。

最近这套方案给我的提效非常明显。比如之前我想做混剪工具，最初想基于开源工具手搓，但针对具体业务场景时，音效库、特效库、字幕格式等细节工程量巨大。自己折腾半个月，可能还不如剪映、开拍这些成熟产品一半好用，毕竟专长不同，人家有一整个团队在打磨。

所以现在的打法是：直接找到合适的平台，通过接口拦截的形式接入能力。

长期商用肯定有一定风险，但我们目前是内部自用，调用量不大，整体完全可接受，提效效果非常显著。